# Datamodule config for WNUT_17 dataset
_target_: datamodules.ner_datamodule.WNUT17DataModule
ner_dataset: wnut_17
num_labels: 13
batch_size: 8
num_workers: 16

# we set following parameters to null because they will be overriden in the 
# main script, and because it is impossible to interpolate values from the 
# defaults list. 
model_type: null
num_splits: null

# Truncation of the dataset for test purposes (default = null)
truncate: null