_target_: src.models.atoa_module.ATOAModule

in_shape: [32, 64, 14]
num_paths: 5
embed_dim: null
w_aoa: 1.0
w_toa: 1.0

# we initialize model in constructor
net: null

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10
